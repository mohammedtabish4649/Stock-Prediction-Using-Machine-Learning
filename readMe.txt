Follow these commands in that order:

/////Change User's Path////

1. Download and install vs code editor
2. open vs code->file->open folder->LOGIN_UI
3. Open vs code powershell.
3.Follow the following commands
python -m venv env
.\env\Scripts\activate
pip install -r .\requirements.txt  
python .\login.py